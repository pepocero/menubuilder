# Sin catch-all /* → index.html (rompe MIME de .js/.css si falta un asset).
# Assets: functions/_middleware.ts via ASSETS.fetch (404 text/plain si faltan).
# SPA: functions/_middleware.ts para el resto de rutas.
