# SPA: rutas de la app → index.html (proxy 200).
# No usar destinos con código 404: Cloudflare Pages no los soporta y
# invalidan el resto de reglas (rompe /p/:slug, /editor/*, etc.).
/*    /index.html   200
